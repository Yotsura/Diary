<Controls:MetroWindow x:Class="Dialy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:Dialy"
        mc:Ignorable="d"
        SaveWindowPosition="True"
        Title="MainWindow" Height="450" Width="800" Loaded="MetroWindow_Loaded">
    <Window.CommandBindings>
        <CommandBinding Command="Save" Executed="SaveCommand" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="Save" Gesture="Ctrl+S"/>
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="5"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Menu Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" Grid.RowSpan="3">
            <MenuItem Header="ファイル(_C)">
                <MenuItem Header="保存" Click="SaveDialy" InputGestureText="Ctrl+S" Command="{Binding Path=Save}"/>
                <MenuItem Header="すべて保存"/>
                <MenuItem Header="今日のレコード"/>
                <MenuItem Header="選択した日のレコードを追加"/>
                <MenuItem Header="選択した日のレコードを削除"/>
            </MenuItem>
            <MenuItem Header="検索(_S)">
                <MenuItem Header="単語を検索"/>
            </MenuItem>
            <MenuItem Header="オプション(_O)">
                <MenuItem IsCheckable="True" Header="最前面表示" />
                <MenuItem Header="設定" />
            </MenuItem>
        </Menu>
        <TextBox Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="2" Name="DialyTxt" AcceptsTab="True" Controls:ScrollViewerHelper.IsHorizontalScrollWheelEnabled="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        <Grid Grid.Column="1" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="LastRecord" Content="&lt;&lt;" Click="DateChangeButton"/>
            <Button Grid.Column="1" Name="LastDay" Content="&lt;" Click="DateChangeButton"/>
            <Button Grid.Column="3" Name="NextDay" Content="&gt;" Click="DateChangeButton"/>
            <Button Grid.Column="4" Name="NextRecord" Content="&gt;&gt;" Click="DateChangeButton"/>
            <DatePicker Grid.Column="2" Name="DatePick" SelectedDateChanged="DatePick_SelectedDateChanged"/>
        </Grid>
        <Grid Grid.Column="2" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Name="ZoomOut" Content="-" Click="FontZoom"/>
            <Button Grid.Column="3" Name="ZoomIn" Content="+" Click="FontZoom"/>
            <Label Grid.Column="2" Name="FontSize" Content="{Binding IndicateSize}"/>
        </Grid>
    </Grid>
</Controls:MetroWindow>
